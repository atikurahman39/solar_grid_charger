# Archive

Earlier drafts of the backend, kept for reference only. Not used at runtime.

- `app_v1.py`, `app_backup.py` — first minimal Flask app (render-only)
- `app_bak2.py` — earlier full API before the final `backend/app.py`
- `database_v1.py` — original single-source schema
- `db_bak2.py` — backup of the current schema

The live versions are in `backend/`.
